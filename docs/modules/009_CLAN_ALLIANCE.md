# Module 09 — Clan & alliance

**User-created orgs** — excluded from static data export.

## Scope

- Create clan, ranks, invite, kick, disband
- Alliance between clans
- `$` and `%` chat (schema exists)
- Shared clan stash (future)
- `users_ig.id_clan`, `id_alliance` sync

## Tables

Use existing `clans`, `alliances`; add `clan_members`, `alliance_clans`.
