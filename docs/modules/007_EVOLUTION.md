# Module 07 — Evolution & DNA

**Depends on:** Solo PvE drops, species static data

## Scope

- DNA fragments per species from wild victories
- Evolution rules table (from `animaster_planning.md`)
- `POST evolve` endpoint + team UI
- Branch evolution (stat-ratio) for at least one line (e.g. dog → wolf)

## Tables

`evolution_rules`, extend `animals` with `dna_count` or separate `animal_dna`
