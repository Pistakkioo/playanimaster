# Copy to .env for local Docker (never commit .env)

# MariaDB
MYSQL_ROOT_PASSWORD=change_me_root
MYSQL_DATABASE=playanimaster_db
MYSQL_USER=animaster
MYSQL_PASSWORD=change_me_app

# App
APP_ENV=local
APP_URL=http://localhost:8080

# Dev-only tools (dev_npcs.php, etc.) — change before exposing Docker to LAN
DEV_ADMIN_TOKEN=change_me_dev_only

# Used by docker-compose only
DB_HOST=db
DB_PORT=3306
DB_NAME=playanimaster_db
DB_USER=animaster
DB_PASSWORD=change_me_app
