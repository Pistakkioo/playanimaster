/**
 * Default wild sprites: 8x8 pixel grid (16px on screen at scale 2).
 */
var AnimasterWildSprites = AnimasterWildSpritesFactory.create({
    variant: 'default',
    defaultPixelSize: 2,
    nearPixelSize: 3
});
